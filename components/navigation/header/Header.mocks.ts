import { IHeader } from './Header';

const base: IHeader = {};

// Header allows rapid prototyping of components, with alt versions, etc.
// const alt: IHeader = {
//   sampleTextProp: 'Hello Alt World!',
// };

export const mockHeaderProps = {
  base,
  //   alt,
};
